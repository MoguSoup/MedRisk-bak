# Remaining optional advanced tabular and local-LLM runtimes. LightGBM,
# CatBoost and TabPFN are installed by the default model-service requirements.
tabicl

# Optional FT-Transformer style research runtime. Integration is advertised as
# unavailable unless a compatible local training wrapper is configured.
rtdl-revisiting-models

# Optional local Hugging Face text generation for the intelligent QA module.
# Install these only on machines intended to load local/Hub LLM weights.
transformers
torch
accelerate
sentencepiece
huggingface_hub
